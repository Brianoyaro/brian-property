const router = require('express').Router();
const connection = require('../config/database');

// create new conversation if non-existent

// get all messages from a given conversation


module.exports = router;